<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title || 'แบบสอบถาม' %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/colors.css">
  <link rel="stylesheet" href="/survey/home.css">
  <link rel="stylesheet" href="/survey/questionnaire.css">
</head>

<body>
  <%- include('partials/desktop-nav', { currentPage: 'questionnaire' }) %>

    <%- include('partials/mobile-nav', { currentPage: 'questionnaire' }) %>

      <!-- Main Content -->
      <main class="container py-4 py-md-5">
        <!-- Header Section -->
        <div class="row mb-4">
          <div class="col-12">
            <div class="page-header">
              <h1 class="page-title">
                <i class="bi bi-clipboard-check text-primary me-2"></i>แบบสอบถาม
              </h1>
              <p class="page-subtitle text-muted mb-0">เลือกแบบสอบถามที่ต้องการตอบ</p>
            </div>
          </div>
        </div>

        <!-- Survey Cards -->
        <div class="row g-4">
          <% if (surveys && surveys.length> 0) { %>
            <% const iconColors=['#3b82f6', '#10b981' , '#f59e0b' , '#8b5cf6' , '#ef4444' , '#06b6d4' ];
              surveys.forEach((survey, index)=> {
              const iconColor = iconColors[index % iconColors.length];
              const statusBadge = survey.status === 'completed' ? 'badge-completed' : 'badge-available';
              const statusText = survey.status === 'completed' ? 'ตอบแล้ว' : 'ว่าง';
              const isCompleted = survey.status === 'completed';
              %>
              <div class="col-12 col-md-6 col-lg-4">
                <div class="survey-card" data-survey-id="<%= survey.id %>">
                  <div class="survey-card-header">
                    <div class="survey-icon" style="background: <%= iconColor %>;">
                      <i class="bi bi-<%= survey.icon || 'clipboard-check' %>"></i>
                    </div>
                    <span class="survey-badge <%= statusBadge %>">
                      <%= statusText %>
                    </span>
                  </div>
                  <div class="survey-card-body">
                    <h5 class="survey-title">
                      <%= survey.title %>
                    </h5>
                    <p class="survey-description">
                      <%= survey.description %>
                    </p>
                    <div class="survey-meta">
                      <div class="meta-item">
                        <i class="bi bi-question-circle me-1"></i>
                        <span>
                          <%= survey.questionCount || (survey.questions ? survey.questions.length : 0) %> คำถาม
                        </span>
                      </div>
                      <div class="meta-item">
                        <i class="bi bi-clock me-1"></i>
                        <span>ใช้เวลา ~<%= survey.duration || '5' %> นาที</span>
                      </div>
                    </div>
                    <% if (isCompleted && survey.completedDate) { %>
                      <div class="completed-info">
                        <i class="bi bi-check-circle-fill text-success me-2"></i>
                        <span>ตอบเมื่อ <%= survey.completedDate %></span>
                      </div>
                      <% } %>
                  </div>
                  <div class="survey-card-footer">
                    <% if (isCompleted) { %>
                      <button class="btn btn-outline-primary w-100" disabled>
                        <i class="bi bi-check-circle me-2"></i>ตอบแล้ว
                      </button>
                      <% } else { %>
                        <a href="/survey/form/<%= survey.id %>" class="btn btn-primary w-100">
                          <i class="bi bi-pencil-square me-2"></i>เริ่มทำแบบสอบถาม
                        </a>
                        <% } %>
                  </div>
                </div>
              </div>
              <% }); %>
                <% } else { %>
                  <!-- Empty State -->
                  <div class="col-12">
                    <div class="empty-state">
                      <i class="bi bi-clipboard-x"></i>
                      <h5>ไม่มีแบบสอบถาม</h5>
                      <p class="text-muted">ขณะนี้ยังไม่มีแบบสอบถามที่เปิดให้ทำ</p>
                    </div>
                  </div>
                  <% } %>
        </div>
      </main>

      <!-- Footer -->
      <footer class="mt-auto py-4 border-top bg-white">
        <div class="container">
          <div class="row">
            <div class="col-12 text-center">
              <p class="text-muted mb-0 small">© 2025 ระบบแบบสอบถามออนไลน์ - สงวนลิขสิทธิ์</p>
            </div>
          </div>
        </div>
      </footer>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
      <script src="/survey/home.js"></script>
      <script src="/survey/questionnaire.js"></script>
</body>

</html>