<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title || 'หน้าหลัก' %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/colors.css">
  <link rel="stylesheet" href="/survey/home.css">
</head>

<body>
  <%- include('partials/desktop-nav', { currentPage: 'home' }) %>

    <%- include('partials/mobile-nav', { currentPage: 'home' }) %>

      <!-- Main Content -->
      <main class="container py-4 py-md-5">
        <!-- Welcome Section -->
        <div class="row mb-4">
          <div class="col-12">
            <div class="welcome-card">
              <h1 class="welcome-title">
                <% if (user?.role === 'student') { %>
                  สวัสดี นักศึกษา
                <% } else if (user?.role === 'teacher') { %>
                  สวัสดี อาจารย์
                <% } else if (user?.role === 'admin') { %>
                  สวัสดี ผู้ดูแลระบบ
                <% } else { %>
                  สวัสดี
                <% } %>
                , <span class="text-primary"><%= user?.name || (user?.prefix || '') + (user?.firstName || '') %></span>
              </h1>
              <p class="welcome-subtitle text-muted mb-0">
                <% const todayCount = stats?.responsesToday ?? 0; const totalCount = stats?.responsesTotal ?? 0; %>
                วันนี้มีผู้ตอบแบบสอบถามแล้ว <strong><%= todayCount %></strong> คน — รวมทั้งหมด <strong><%= totalCount %></strong> ครั้ง
              </p>
            </div>
          </div>
        </div>

        <!-- Student Information Card -->
        <div class="row">
          <div class="col-12">
            <div class="card border-0 shadow-sm">
              <div class="card-header bg-white border-bottom py-3">
                <h5 class="mb-0 fw-semibold">
                  <i class="bi bi-person-badge text-primary me-2"></i>ข้อมูลนักศึกษา
                </h5>
              </div>
              <div class="card-body p-4">
                <div class="row g-4">
                  <!-- Profile Image -->
                  <div class="col-12 col-md-4 col-lg-3 text-center">
                    <div class="profile-avatar mx-auto">
                      <i class="bi bi-person-circle"></i>
                    </div>
                    <div class="mt-3">
                      <span class="badge bg-primary px-3 py-2">นักศึกษา</span>
                    </div>
                  </div>

                  <!-- Student Details -->
                  <div class="col-12 col-md-8 col-lg-9">
                    <div class="row g-3">
                      <div class="col-12">
                        <div class="info-item">
                          <label class="info-label">ชื่อ-นามสกุล</label>
                          <p class="info-value">
                            <%= user?.name || (user?.prefix || '') + (user?.firstName || '') + ' ' + (user?.lastName || '') %>
                          </p>
                        </div>
                      </div>
                      <div class="col-12 col-sm-6">
                        <div class="info-item">
                          <label class="info-label">อีเมล</label>
                          <p class="info-value">
                            <%= user?.email || 'student@example.com' %>
                          </p>
                        </div>
                      </div>
                      <div class="col-12 col-sm-6">
                        <div class="info-item">
                          <label class="info-label">คณะ</label>
                          <p class="info-value text-muted">
                            <i class="bi bi-lock-fill me-1"></i>คณะครุศาสตร์
                          </p>
                        </div>
                      </div>
                      <div class="col-12 col-sm-6">
                        <div class="info-item">
                          <label class="info-label">สาขาวิชา <% if (!user?.major || user?.major === 'ยังไม่ระบุ') { %><span class="badge bg-warning text-dark">กรุณาเลือก</span><% } %></label>
                          <% if (user?.major && user?.major !== 'ยังไม่ระบุ') { %>
                            <p class="info-value">
                              <i class="bi bi-check-circle-fill text-success me-1"></i><%= user.major %>
                            </p>
                          <% } else { %>
                            <div class="d-flex gap-2 align-items-center">
                              <select class="form-select form-select-sm" id="majorSelect" style="max-width: 300px;">
                                <option value="">-- เลือกสาขาวิชา --</option>
                                <option value="การศึกษาปฐมวัย">การศึกษาปฐมวัย</option>
                                <option value="วิทยาศาสตร์ทั่วไป">วิทยาศาสตร์ทั่วไป</option>
                                <option value="คณิตศาสตร์">คณิตศาสตร์</option>
                                <option value="ภาษาอังกฤษ">ภาษาอังกฤษ</option>
                                <option value="ภาษาไทย">ภาษาไทย</option>
                                <option value="สังคมศึกษา">สังคมศึกษา</option>
                                <option value="พลศึกษา">พลศึกษา</option>
                                <option value="เทคโนโลยีดิจิทัลเพื่อการศึกษา">เทคโนโลยีดิจิทัลเพื่อการศึกษา</option>
                                <option value="การประถมศึกษา">การประถมศึกษา</option>
                              </select>
                              <button type="button" class="btn btn-primary btn-sm" id="saveMajorBtn" disabled>
                                <i class="bi bi-save me-1"></i>บันทึก
                              </button>
                            </div>
                          <% } %>
                        </div>
                      </div>
                      <div class="col-12 col-sm-6">
                        <div class="info-item">
                          <label class="info-label">ชั้นปีที่</label>
                          <p class="info-value text-muted">
                            <i class="bi bi-lock-fill me-1"></i>4
                          </p>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Quick Actions -->
        <div class="row mt-4">
          <div class="col-12">
            <h5 class="mb-3 fw-semibold">สถิติการทำแบบสอบถาม</h5>
          </div>
          <div class="col-12 col-md-4 mb-3">
            <div class="card border-0 shadow-sm h-100">
              <div class="card-body text-center p-4">
                <div class="mb-3">
                  <i class="bi bi-check-circle-fill text-success" style="font-size: 3rem;"></i>
                </div>
                <h3 class="mb-1"><%= stats?.completed || 0 %></h3>
                <p class="text-muted mb-0">ทำแล้ว</p>
              </div>
            </div>
          </div>
          <div class="col-12 col-md-4 mb-3">
            <div class="card border-0 shadow-sm h-100">
              <div class="card-body text-center p-4">
                <div class="mb-3">
                  <i class="bi bi-clipboard-check text-primary" style="font-size: 3rem;"></i>
                </div>
                <h3 class="mb-1"><%= stats?.total || 0 %></h3>
                <p class="text-muted mb-0">ทั้งหมด</p>
              </div>
            </div>
          </div>
          <div class="col-12 col-md-4 mb-3">
            <div class="card border-0 shadow-sm h-100">
              <div class="card-body text-center p-4">
                <div class="mb-3">
                  <i class="bi bi-hourglass-split text-warning" style="font-size: 3rem;"></i>
                </div>
                <h3 class="mb-1"><%= stats?.remaining || 0 %></h3>
                <p class="text-muted mb-0">เหลืออยู่</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Quick Actions Menu -->
        <div class="row mt-4">
          <div class="col-12">
            <h5 class="mb-3 fw-semibold">เมนูด่วน</h5>
          </div>
          <div class="col-12 col-md-6 col-lg-4 mb-3">
            <a href="/survey/questionnaire" class="quick-action-card">
              <div class="quick-action-icon bg-primary">
                <i class="bi bi-clipboard-check"></i>
              </div>
              <div class="quick-action-content">
                <h6 class="quick-action-title">ทำแบบสอบถาม</h6>
                <p class="quick-action-text">เริ่มตอบแบบสอบถาม</p>
              </div>
              <i class="bi bi-chevron-right quick-action-arrow"></i>
            </a>
          </div>
          <div class="col-12 col-md-6 col-lg-4 mb-3">
            <a href="/survey/questionnaire" class="quick-action-card">
              <div class="quick-action-icon" style="background: var(--color-secondary-1);">
                <i class="bi bi-clock-history text-primary"></i>
              </div>
              <div class="quick-action-content">
                <h6 class="quick-action-title">ประวัติการทำแบบสอบถาม</h6>
                <p class="quick-action-text">ดูแบบสอบถามที่ทำไปแล้ว</p>
              </div>
              <i class="bi bi-chevron-right quick-action-arrow"></i>
            </a>
          </div>
          <div class="col-12 col-md-6 col-lg-4 mb-3">
            <a href="/survey/settings" class="quick-action-card">
              <div class="quick-action-icon" style="background: var(--color-secondary-1);">
                <i class="bi bi-gear text-primary"></i>
              </div>
              <div class="quick-action-content">
                <h6 class="quick-action-title">ตั้งค่าบัญชี</h6>
                <p class="quick-action-text">จัดการข้อมูลส่วนตัว</p>
              </div>
              <i class="bi bi-chevron-right quick-action-arrow"></i>
            </a>
          </div>
        </div>
      </main>

      <!-- Footer -->
      <footer class="mt-auto py-4 border-top bg-white">
        <div class="container">
          <div class="row">
            <div class="col-12 text-center">
              <p class="text-muted mb-0 small">© 2025 ระบบแบบสอบถามออนไลน์ - สงวนลิขสิทธิ์</p>
            </div>
          </div>
        </div>
      </footer>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
      <script src="/survey/home.js"></script>
</body>

</html>