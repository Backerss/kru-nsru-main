<!-- Mobile Bottom Navigation -->
<nav class="mobile-bottom-nav">
    <a href="/survey/home" class="mobile-nav-item <%= currentPage === 'home' ? 'active' : '' %>">
        <i class="bi bi-house-door<%= currentPage === 'home' ? '-fill' : '' %>"></i>
        <span>หน้าหลัก</span>
    </a>
    <a href="/survey/questionnaire" class="mobile-nav-item <%= currentPage === 'questionnaire' ? 'active' : '' %>">
        <i class="bi bi-clipboard-check<%= currentPage === 'questionnaire' ? '-fill' : '' %>"></i>
        <span>แบบสอบถาม</span>
    </a>
    <% if (user && (user.role === 'admin' || user.role === 'teacher')) { %>
    <a href="/admin" class="mobile-nav-item <%= currentPage === 'admin' ? 'active' : '' %>">
        <i class="bi bi-speedometer2<%= currentPage === 'admin' ? '-fill' : '' %>"></i>
        <span>แผงควบคุม</span>
    </a>
    <% } %>
    <a href="/survey/settings" class="mobile-nav-item <%= currentPage === 'settings' ? 'active' : '' %>">
        <i class="bi bi-gear<%= currentPage === 'settings' ? '-fill' : '' %>"></i>
        <span>ตั้งค่า</span>
    </a>
    <a href="#" class="mobile-nav-item" data-logout>
        <i class="bi bi-box-arrow-right"></i>
        <span>ออกจากระบบ</span>
    </a>
</nav>