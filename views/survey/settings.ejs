<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'ตั้งค่า' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/colors.css">
  <link rel="stylesheet" href="/survey/home.css">
  <link rel="stylesheet" href="/survey/settings.css">
</head>
<body>
  <!-- Desktop Navigation -->
  <nav class="navbar navbar-light bg-white border-bottom sticky-top desktop-navbar">
    <div class="container-fluid px-3 px-md-4">
      <a class="navbar-brand fw-bold text-primary" href="/survey/home">
        <i class="bi bi-mortarboard-fill me-2"></i>ระบบแบบสอบถาม
      </a>
      <ul class="navbar-nav flex-row gap-3">
        <li class="nav-item">
          <a class="nav-link" href="/survey/home">
            <i class="bi bi-house-door me-1"></i>หน้าหลัก
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/survey/questionnaire">
            <i class="bi bi-clipboard-check me-1"></i>แบบสอบถาม
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/survey/settings">
            <i class="bi bi-gear me-1"></i>ตั้งค่า
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-muted" href="/login">
            <i class="bi bi-box-arrow-right me-1"></i>ออกจากระบบ
          </a>
        </li>
      </ul>
    </div>
  </nav>

  <!-- Mobile Bottom Navigation -->
  <nav class="mobile-bottom-nav">
    <a href="/survey/home" class="mobile-nav-item">
      <i class="bi bi-house-door"></i>
      <span>หน้าหลัก</span>
    </a>
    <a href="/survey/questionnaire" class="mobile-nav-item">
      <i class="bi bi-clipboard-check"></i>
      <span>แบบสอบถาม</span>
    </a>
    <a href="/survey/settings" class="mobile-nav-item active">
      <i class="bi bi-gear-fill"></i>
      <span>ตั้งค่า</span>
    </a>
    <a href="/login" class="mobile-nav-item">
      <i class="bi bi-box-arrow-right"></i>
      <span>ออกจากระบบ</span>
    </a>
  </nav>

  <!-- Main Content -->
  <main class="container py-4 py-md-5">
    <!-- Header Section -->
    <div class="row mb-4">
      <div class="col-12">
        <div class="page-header">
          <h1 class="page-title">
            <i class="bi bi-gear text-primary me-2"></i>ตั้งค่าบัญชี
          </h1>
          <p class="page-subtitle text-muted mb-0">จัดการข้อมูลส่วนตัวและความปลอดภัย</p>
        </div>
      </div>
    </div>

    <!-- Alert Messages -->
    <div id="alertContainer"></div>

    <!-- Personal Information Section -->
    <div class="row mb-4">
      <div class="col-12">
        <div class="settings-card">
          <div class="settings-card-header">
            <h5 class="settings-card-title">
              <i class="bi bi-person-circle me-2"></i>ข้อมูลส่วนตัว
            </h5>
            <button class="btn btn-sm btn-outline-primary" id="editPersonalBtn">
              <i class="bi bi-pencil me-1"></i>แก้ไข
            </button>
          </div>
          <div class="settings-card-body">
            <form id="personalInfoForm">
              <div class="row g-3">
                <!-- Student ID (Read-only) -->
                <div class="col-12 col-md-6">
                  <label class="form-label">รหัสนักศึกษา</label>
                  <input type="text" class="form-control" value="<%= user?.studentId || '6510000000' %>" readonly>
                  <small class="text-muted">ไม่สามารถแก้ไขได้</small>
                </div>

                <!-- Email (Read-only) -->
                <div class="col-12 col-md-6">
                  <label class="form-label">อีเมล</label>
                  <input type="email" class="form-control" value="<%= user?.email || 'student@example.com' %>" readonly>
                  <small class="text-muted">ไม่สามารถแก้ไขได้</small>
                </div>

                <!-- First Name (Editable) -->
                <div class="col-12 col-md-6">
                  <label class="form-label required">ชื่อจริง</label>
                  <input type="text" class="form-control" id="firstName" name="firstName" value="ทดสอบ" disabled required>
                </div>

                <!-- Last Name (Editable) -->
                <div class="col-12 col-md-6">
                  <label class="form-label required">นามสกุล</label>
                  <input type="text" class="form-control" id="lastName" name="lastName" value="ระบบ" disabled required>
                </div>

                <!-- Faculty (Read-only) -->
                <div class="col-12 col-md-6">
                  <label class="form-label">คณะ</label>
                  <input type="text" class="form-control" value="<%= user?.faculty || 'คณะวิทยาศาสตร์' %>" readonly>
                  <small class="text-muted">ติดต่อเจ้าหน้าที่เพื่อแก้ไข</small>
                </div>

                <!-- Major (Read-only) -->
                <div class="col-12 col-md-6">
                  <label class="form-label">สาขาวิชา</label>
                  <input type="text" class="form-control" value="<%= user?.major || 'วิทยาการคอมพิวเตอร์' %>" readonly>
                  <small class="text-muted">ติดต่อเจ้าหน้าที่เพื่อแก้ไข</small>
                </div>

                <!-- Year (Read-only) -->
                <div class="col-12 col-md-6">
                  <label class="form-label">ชั้นปีที่</label>
                  <input type="text" class="form-control" value="<%= user?.year || '4' %>" readonly>
                  <small class="text-muted">ติดต่อเจ้าหน้าที่เพื่อแก้ไข</small>
                </div>
              </div>

              <!-- Action Buttons (Hidden by default) -->
              <div class="form-actions d-none" id="personalActions">
                <button type="submit" class="btn btn-primary">
                  <i class="bi bi-check-circle me-2"></i>บันทึกการเปลี่ยนแปลง
                </button>
                <button type="button" class="btn btn-outline-secondary" id="cancelPersonalBtn">
                  <i class="bi bi-x-circle me-2"></i>ยกเลิก
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- Change Password Section -->
    <div class="row mb-4">
      <div class="col-12">
        <div class="settings-card">
          <div class="settings-card-header">
            <h5 class="settings-card-title">
              <i class="bi bi-shield-lock me-2"></i>เปลี่ยนรหัสผ่าน
            </h5>
          </div>
          <div class="settings-card-body">
            <form id="changePasswordForm">
              <div class="row g-3">
                <!-- Current Password -->
                <div class="col-12">
                  <label class="form-label required">รหัสผ่านปัจจุบัน</label>
                  <div class="password-input-group">
                    <input type="password" class="form-control" id="currentPassword" name="currentPassword" required>
                    <button type="button" class="password-toggle" data-target="currentPassword">
                      <i class="bi bi-eye"></i>
                    </button>
                  </div>
                </div>

                <!-- New Password -->
                <div class="col-12 col-md-6">
                  <label class="form-label required">รหัสผ่านใหม่</label>
                  <div class="password-input-group">
                    <input type="password" class="form-control" id="newPassword" name="newPassword" minlength="6" required>
                    <button type="button" class="password-toggle" data-target="newPassword">
                      <i class="bi bi-eye"></i>
                    </button>
                  </div>
                  <small class="text-muted">รหัสผ่านต้องมีอย่างน้อย 6 ตัวอักษร</small>
                </div>

                <!-- Confirm New Password -->
                <div class="col-12 col-md-6">
                  <label class="form-label required">ยืนยันรหัสผ่านใหม่</label>
                  <div class="password-input-group">
                    <input type="password" class="form-control" id="confirmNewPassword" name="confirmNewPassword" minlength="6" required>
                    <button type="button" class="password-toggle" data-target="confirmNewPassword">
                      <i class="bi bi-eye"></i>
                    </button>
                  </div>
                </div>
              </div>

              <!-- Action Buttons -->
              <div class="form-actions">
                <button type="submit" class="btn btn-primary">
                  <i class="bi bi-shield-check me-2"></i>เปลี่ยนรหัสผ่าน
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- Contact Admin Section -->
    <div class="row">
      <div class="col-12">
        <div class="contact-admin-card">
          <div class="contact-icon">
            <i class="bi bi-person-badge"></i>
          </div>
          <div class="contact-content">
            <h6 class="contact-title">ต้องการแก้ไขข้อมูลอื่นๆ?</h6>
            <p class="contact-text">หากต้องการแก้ไขข้อมูลอื่นๆ เช่น คณะ สาขาวิชา หรือชั้นปี กรุณาติดต่อเจ้าหน้าที่</p>
            <div class="contact-info">
              <div class="contact-item">
                <i class="bi bi-person-circle text-primary me-2"></i>
                <span><strong>อาจารย์อาสาฬ รอดนวน</strong></span>
              </div>
              <div class="contact-item">
                <i class="bi bi-envelope text-primary me-2"></i>
                <span>asan.rodnavon@example.com</span>
              </div>
              <div class="contact-item">
                <i class="bi bi-telephone text-primary me-2"></i>
                <span>02-xxx-xxxx</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="mt-auto py-4 border-top bg-white">
    <div class="container">
      <div class="row">
        <div class="col-12 text-center">
          <p class="text-muted mb-0 small">© 2025 ระบบแบบสอบถามออนไลน์ - สงวนลิขสิทธิ์</p>
        </div>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/survey/home.js"></script>
  <script src="/survey/settings.js"></script>
</body>
</html>
