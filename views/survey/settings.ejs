<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title || 'ตั้งค่า' %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/colors.css">
  <link rel="stylesheet" href="/survey/home.css">
  <link rel="stylesheet" href="/survey/settings.css">
</head>

<body>
  <%- include('partials/desktop-nav', { currentPage: 'settings' }) %>

    <%- include('partials/mobile-nav', { currentPage: 'settings' }) %>

      <!-- Main Content -->
      <main class="container py-4 py-md-5">
        <!-- Header Section -->
        <div class="row mb-4">
          <div class="col-12">
            <div class="page-header">
              <h1 class="page-title">
                <i class="bi bi-gear text-primary me-2"></i>ตั้งค่าบัญชี
              </h1>
              <p class="page-subtitle text-muted mb-0">จัดการข้อมูลส่วนตัวและความปลอดภัย</p>
            </div>
          </div>
        </div>

        <!-- Alert Messages -->
        <div id="alertContainer"></div>

        <!-- Personal Information Section -->
        <div class="row mb-4">
          <div class="col-12">
            <div class="settings-card">
              <div class="settings-card-header">
                <h5 class="settings-card-title">
                  <i class="bi bi-person-circle me-2"></i>ข้อมูลส่วนตัว
                </h5>
                <button class="btn btn-sm btn-outline-primary" id="editPersonalBtn">
                  <i class="bi bi-pencil me-1"></i>แก้ไข
                </button>
              </div>
              <div class="settings-card-body">
                <form id="personalInfoForm">
                  <div class="row g-3">
                    <!-- Email (Read-only with disconnect option for Google OAuth) -->
                    <div class="col-12">
                      <label class="form-label">อีเมล</label>
                      <div class="position-relative">
                        <input type="email" class="form-control" value="<%= user?.email || 'student@example.com' %>"
                          readonly>
                        <% if (user?.authProvider === 'google') { %>
                        <button type="button" class="btn btn-sm btn-link text-danger position-absolute" 
                          id="disconnectGoogleBtn"
                          style="top: 50%; right: 10px; transform: translateY(-50%); padding: 0; width: 24px; height: 24px;"
                          title="ยกเลิกการเชื่อมต่อ Google และตั้งรหัสผ่านใหม่">
                          <i class="bi bi-x-circle-fill"></i>
                        </button>
                        <% } %>
                      </div>
                      <small class="text-muted">
                        <% if (user?.authProvider === 'google') { %>
                          <i class="bi bi-google me-1"></i>เชื่อมต่อผ่าน Google (คลิก X เพื่อยกเลิกและตั้งรหัสผ่าน)
                        <% } else { %>
                          ไม่สามารถแก้ไขได้
                        <% } %>
                      </small>
                    </div>

                    <!-- Prefix -->
                    <div class="col-12 col-md-3">
                      <label class="form-label">คำนำหน้า</label>
                      <select class="form-select" id="prefix" name="prefix" disabled>
                        <option value="">ไม่ระบุ</option>
                        <option value="นาย" <%= user?.prefix === 'นาย' ? 'selected' : '' %>>นาย</option>
                        <option value="นางสาว" <%= user?.prefix === 'นางสาว' ? 'selected' : '' %>>นางสาว</option>
                        <option value="นาง" <%= user?.prefix === 'นาง' ? 'selected' : '' %>>นาง</option>
                      </select>
                    </div>

                    <!-- First Name (Editable) -->
                    <div class="col-12 col-md-4">
                      <label class="form-label required">ชื่อจริง</label>
                      <input type="text" class="form-control" id="firstName" name="firstName" value="<%= user?.firstName || '' %>" disabled
                        required>
                    </div>

                    <!-- Last Name (Editable) -->
                    <div class="col-12 col-md-5">
                      <label class="form-label required">นามสกุล</label>
                      <input type="text" class="form-control" id="lastName" name="lastName" value="<%= user?.lastName || '' %>" disabled
                        required>
                    </div>

                    <!-- Faculty (Locked to คณะครุศาสตร์) -->
                    <div class="col-12 col-md-6">
                      <label class="form-label">คณะ</label>
                      <div class="d-flex align-items-center">
                        <span class="text-muted me-2">🔒</span>
                        <input type="text" class="form-control" value="คณะครุศาสตร์" readonly>
                      </div>
                      <small class="text-muted">ข้อมูลถูกกำหนดโดยระบบ</small>
                    </div>

                    <!-- Year (Locked to 4) -->
                    <div class="col-12 col-md-6">
                      <label class="form-label">ชั้นปี</label>
                      <div class="d-flex align-items-center">
                        <span class="text-muted me-2">🔒</span>
                        <input type="text" class="form-control" value="4" readonly>
                      </div>
                      <small class="text-muted">ข้อมูลถูกกำหนดโดยระบบ</small>
                    </div>

                    <!-- Major (One-time selection) -->
                    <div class="col-12">
                      <label class="form-label">สาขาวิชา <% if (!user?.major || user.major === 'ยังไม่ระบุ') { %><span class="badge bg-warning text-dark ms-2 pulse-badge">เลือกได้ครั้งเดียว</span><% } %></label>
                      <% if (user?.major && user.major !== 'ยังไม่ระบุ') { %>
                        <div class="d-flex align-items-center">
                          <span class="text-success me-2">✓</span>
                          <input type="text" class="form-control" value="<%= user.major %>" readonly>
                        </div>
                        <small class="text-muted">คุณได้เลือกสาขาวิชาแล้ว ไม่สามารถเปลี่ยนแปลงได้</small>
                      <% } else { %>
                        <select class="form-select" id="major" name="major" disabled>
                          <option value="">-- กรุณาเลือกสาขาวิชา --</option>
                          <option value="การศึกษาปฐมวัย">การศึกษาปฐมวัย</option>
                          <option value="วิทยาศาสตร์ทั่วไป">วิทยาศาสตร์ทั่วไป</option>
                          <option value="คณิตศาสตร์">คณิตศาสตร์</option>
                          <option value="ภาษาอังกฤษ">ภาษาอังกฤษ</option>
                          <option value="ภาษาไทย">ภาษาไทย</option>
                          <option value="สังคมศึกษา">สังคมศึกษา</option>
                          <option value="พลศึกษา">พลศึกษา</option>
                          <option value="เทคโนโลยีดิจิทัลเพื่อการศึกษา">เทคโนโลยีดิจิทัลเพื่อการศึกษา</option>
                          <option value="การประถมศึกษา">การประถมศึกษา</option>
                        </select>
                        <small class="text-muted">⚠️ สาขาวิชาที่เลือกไม่สามารถเปลี่ยนแปลงได้ กรุณาเลือกอย่างรอบคอบ</small>
                      <% } %>
                    </div>
                  </div>

                  <!-- Action Buttons (Hidden by default) -->
                  <div class="form-actions d-none" id="personalActions">
                    <button type="submit" class="btn btn-primary">
                      <i class="bi bi-check-circle me-2"></i>บันทึกการเปลี่ยนแปลง
                    </button>
                    <button type="button" class="btn btn-outline-secondary" id="cancelPersonalBtn">
                      <i class="bi bi-x-circle me-2"></i>ยกเลิก
                    </button>
                  </div>
                </form>
              </div>
            </div>
          </div>
        </div>

        <!-- Change Password Section (Hidden for Google OAuth users) -->
        <% if (user?.authProvider !== 'google') { %>
        <div class="row mb-4">
          <div class="col-12">
            <div class="settings-card">
              <div class="settings-card-header">
                <h5 class="settings-card-title">
                  <i class="bi bi-shield-lock me-2"></i>เปลี่ยนรหัสผ่าน
                </h5>
              </div>
              <div class="settings-card-body">
                  <!-- Regular users can change password -->
                  <form id="changePasswordForm">
                    <div class="row g-3">
                      <!-- Current Password -->
                      <div class="col-12">
                        <label class="form-label required">รหัสผ่านปัจจุบัน</label>
                        <div class="password-input-group">
                          <input type="password" class="form-control" id="currentPassword" name="currentPassword"
                            required>
                          <button type="button" class="password-toggle" data-target="currentPassword">
                            <i class="bi bi-eye"></i>
                          </button>
                        </div>
                      </div>

                      <!-- New Password -->
                      <div class="col-12 col-md-6">
                        <label class="form-label required">รหัสผ่านใหม่</label>
                        <div class="password-input-group">
                          <input type="password" class="form-control" id="newPassword" name="newPassword" minlength="6"
                            required>
                          <button type="button" class="password-toggle" data-target="newPassword">
                            <i class="bi bi-eye"></i>
                          </button>
                        </div>
                        <small class="text-muted">รหัสผ่านต้องมีอย่างน้อย 6 ตัวอักษร</small>
                      </div>

                      <!-- Confirm New Password -->
                      <div class="col-12 col-md-6">
                        <label class="form-label required">ยืนยันรหัสผ่านใหม่</label>
                        <div class="password-input-group">
                          <input type="password" class="form-control" id="confirmNewPassword" name="confirmNewPassword"
                            minlength="6" required>
                          <button type="button" class="password-toggle" data-target="confirmNewPassword">
                            <i class="bi bi-eye"></i>
                          </button>
                        </div>
                      </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="form-actions">
                      <button type="submit" class="btn btn-primary">
                        <i class="bi bi-shield-check me-2"></i>เปลี่ยนรหัสผ่าน
                      </button>
                    </div>
                  </form>
              </div>
            </div>
          </div>
        </div>
        <% } %>

        <!-- Contact Admin Section -->
        <div class="row">
          <div class="col-12">
            <div class="contact-admin-card">
              <div class="contact-icon">
                <i class="bi bi-person-badge"></i>
              </div>
              <div class="contact-content">
                <h6 class="contact-title">ต้องการแก้ไขข้อมูลอื่นๆ?</h6>
                <p class="contact-text">หากต้องการแก้ไขข้อมูลอื่นๆ เช่น คณะ สาขาวิชา หรือชั้นปี กรุณาติดต่อเจ้าหน้าที่
                </p>
                <div class="contact-info">
                  <div class="contact-item">
                    <i class="bi bi-person-circle text-primary me-2"></i>
                    <span><strong>นาย อาสาฬ รอดนวน</strong></span>
                  </div>
                  <div class="contact-item">
                    <i class="bi bi-envelope text-primary me-2"></i>
                    <span>asan.r@nsru.ac.th</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>

      <!-- Footer -->
      <footer class="mt-auto py-4 border-top bg-white">
        <div class="container">
          <div class="row">
            <div class="col-12 text-center">
              <p class="text-muted mb-0 small">© 2025 ระบบแบบสอบถามออนไลน์ - สงวนลิขสิทธิ์</p>
            </div>
          </div>
        </div>
      </footer>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
      <script src="/survey/home.js"></script>
      <script src="/survey/settings.js"></script>
</body>

</html>